apiVersion: v1
kind: Service
metadata:
  labels:
    app: docker-flask-demo-app
  name: docker-flask-demo-service
spec:
  ports:
    - name: 8080-tcp
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    name: docker-flask-demo-app
  type: ClusterIP